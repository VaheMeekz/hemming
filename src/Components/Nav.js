import React from 'react';
import {Container, Navbar} from "react-bootstrap";

const Nav = () => {
    return (
        <div>
            <Navbar bg="dark">
                <Container>
                    <Navbar.Brand>
                        <h2></h2>
                    </Navbar.Brand>
                </Container>
            </Navbar>
        </div>
    );
};

export default Nav;